# Registre public épinglé : le lockfile doit rester installable par la CI et le VPS,
# même quand `npm install` est lancé depuis un poste réglé sur un registre d'entreprise.
registry=https://registry.npmjs.org/
